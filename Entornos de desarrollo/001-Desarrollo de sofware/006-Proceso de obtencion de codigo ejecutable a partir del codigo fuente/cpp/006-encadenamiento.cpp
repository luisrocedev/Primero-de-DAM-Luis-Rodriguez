#include <iostream> // Incluye una libreria de fuljo (stream) de entrada (input) y salida (out)

int main()
{ // Creo una funcion, pero es el punto de entrada de la ejecuccion
    int edad = 46.00;
    std::string nombre = "Luis Rodriguez";                                                               // De la libreria std, importo el tipo de dato string, y creo una variable
    std::cout << "Hola mundo desde C++, me llamo " << nombre << " Y estoy muy contendo de aprender C++"; // De la libreria estanda (std) quiero usar el comando C-exe, y saco una
    return 0;                                                                                            // Cumplo la promesa de devolver un entero
}
